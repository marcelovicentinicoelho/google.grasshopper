import { width, height } from 'monitor.resolution';
function totalPixels(width, height, ___) {
  return width * height;
};
console.log(width + ' by ' + height);
console.log(totalPixels(width, height));
